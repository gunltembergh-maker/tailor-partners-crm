

# Replace QuantitativoTab.tsx with uploaded version

## Summary
Replace `src/components/dashboard/QuantitativoTab.tsx` (548 lines) with the uploaded version (597 lines).

Key differences in the new version:
- Added `CASA_COLORS` map for fixed colors per "casa"
- New `fmtBRLFull` function for full BRL formatting
- Updated `fmtBRL` with abbreviated "Mi" format for millions
- Minor tooltip and treemap styling refinements
- All existing hooks and data logic preserved

## Change

| File | Action |
|---|---|
| `src/components/dashboard/QuantitativoTab.tsx` | Overwrite with uploaded 597-line file |

No other files affected.


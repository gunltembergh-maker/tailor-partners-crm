
# Replace ImportarBases.tsx with uploaded file

## Summary
Replace the entire `src/pages/ImportarBases.tsx` with the user-uploaded file (632 lines). The new version has a cleaner architecture with typed `BaseConfig`/`SheetMapping` structs, a dropzone UI with per-sheet status tracking, and simplified truncate-and-insert logic.

## Changes

| File | Action |
|---|---|
| `src/pages/ImportarBases.tsx` | Overwrite entirely with uploaded 632-line file |

No other files need changes.
